package internal

import (
	"testing"
)

type ANode struct {
	t *testing.T
}

//func (n ANode) CmpSelectField(pnode pnode.FieldNode, anode anode.SelectField) {
//	assert.Eq(pnode.FieldName, anode.Column.Name(), n.t)
//	assert.Eq(pnode.FieldAlias, anode.FieldAlias, n.t)
//	assert.Eq(pnode.TableAlias, anode.)
//}
